package org.example.tree_implimentation;

//Max Heap: The root node is always the largest value, and every parent node is greater than its children.

//Min Heap: The root node is always the smallest value, and every parent node is smaller than its children.

public class Max_heap {
}
